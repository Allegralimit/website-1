---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
).slice(0, 3);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<a href="https://www.linkedin.com/in/mayukhee-das/" target="_blank" rel="noopener noreferrer">
					<img src="/profile.jpg" alt="Mayukhee" class="profile-pic" />
				</a>
				<h1>Hi, I'm Mayukhee 👋</h1>
				<p>Welcome to my corner of the internet. I write, build, and share things here.</p>
			</section>

			<section>
				<h2>Recent Posts</h2>
				<ul class="post-list">
					{posts.map((post) => (
						<li>
							<a href={`/blog/${post.id}/`}>
								<h3>{post.data.title}</h3>
								<FormattedDate date={post.data.pubDate} />
								<p>{post.data.description}</p>
							</a>
						</li>
					))}
				</ul>
				<p><a href="/blog">View all posts &rarr;</a></p>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.profile-pic {
		width: 150px;
		height: 150px;
		border-radius: 50%;
		object-fit: cover;
		margin-bottom: 1rem;
		border: 3px solid rgb(var(--gray-light));
		transition: border-color 0.2s;
	}
	.profile-pic:hover {
		border-color: var(--accent);
	}
	.hero {
		padding: 3rem 0 2rem;
		border-bottom: 1px solid rgb(var(--gray-light));
		margin-bottom: 2rem;
		text-align: center;
	}
	.hero h1 {
		font-size: 2.5rem;
		margin-bottom: 0.5rem;
	}
	.hero p {
		color: rgb(var(--gray));
		font-size: 1.15rem;
	}
	h2 {
		font-size: 1.1rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		color: rgb(var(--gray));
		margin-bottom: 1.5rem;
	}
	.post-list {
		list-style: none;
		padding: 0;
	}
	.post-list li {
		margin-bottom: 1.5rem;
		padding-bottom: 1.5rem;
		border-bottom: 1px solid rgb(var(--gray-light));
	}
	.post-list a {
		text-decoration: none;
		color: inherit;
	}
	.post-list a:hover h3 {
		color: var(--accent);
	}
	.post-list h3 {
		font-size: 1.3rem;
		margin-bottom: 0.2rem;
		transition: color 0.2s;
	}
	.post-list time {
		font-size: 0.85rem;
		color: rgb(var(--gray));
	}
	.post-list p {
		color: rgb(var(--gray));
		margin-top: 0.3rem;
	}
</style>
